<h1>Editing Post</h1>


<%= render 'form' %>
  
  <strong>Image:</strong>
<%= image_tag @post.image.url(:thumb) %>
</p>

<%= form_for @post, url: {action: 'remove_image'}, method: 'post' do |f| %>
 	<%= f.hidden_field :id, :value => "#{@post.id}" %>
  <%=f.submit "Delete Image" %>
<% end %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
